{
  "name": "scraphand",
  "private": true,
  "description": "Scraphand language monorepo (engine + VS Code extension)",
  "author": "Simon Walker",
  "license": "MIT",

  "workspaces": [
    "engine",
    "vscode"
  ],

  "scripts": {
    "build": "npm run build -w engine",
    "clean": "npm run clean -w engine",
    "build:all": "npm run build -w engine && npm run build -w vscode",
    "install:all": "npm install",
    "dev:engine": "npm run dev -w engine"
  }
}
