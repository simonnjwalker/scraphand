{
  "name": "engine",
  "version": "0.2.0",
  "description": "Core language engine for the Scraphand (.sh1) language",
  "author": "Simon Walker",
  "license": "MIT",

  "main": "dist/index.js",
  "types": "dist/index.d.ts",

  "files": [
    "dist"
  ],

  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rmdir /s /q dist",
    "dev": "npm run build && node dist/dev-test.js"
  },

  "dependencies": {
    "sh1-runtime": "*"
  },

  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
